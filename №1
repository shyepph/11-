print("Все тройки:")
for n in range(0, 13):
    for k in range(0, 13 - n):
        total_days = 365 - (28 * n + 30 * k)
        if total_days >= 0 and total_days <= 31:
            m = total_days / 31
            if m >= 0:
                print(f"n = {n}, k = {k}, m = {m}")
